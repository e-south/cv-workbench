project:
  name: cv-workbench
paths:
  sot: ../cv-sot
  dist: ../dist
  runs: ../runs
  registry: ../registry
variants:
  default: base
render:
  pdf_engine: xelatex
site:
  sync_mode: local
registry:
  user_agent: cv-workbench/0.1
