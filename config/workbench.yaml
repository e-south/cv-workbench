project:
  name: cv-workbench
paths:
  sot: ../sot
  dist: ../dist
  runs: ../runs
  drafts: ../drafts
  reviews: ../reviews
  registry: ../registry
variants:
  default: base
render:
  themes_dir: ../build/themes
  theme: default
  style_preset: modern
  pdf_engine: xelatex
site:
  sync_mode: local
registry:
  user_agent: cv-workbench/0.1
