project:
  name: cv-workbench
paths:
  sot: ../sot
  dist: ../dist
  runs: ../runs
  drafts: ../drafts
  reviews: ../reviews
  registry: ../registry
variants:
  default: base
render:
  pdf_engine: xelatex
site:
  sync_mode: local
registry:
  user_agent: cv-workbench/0.1
